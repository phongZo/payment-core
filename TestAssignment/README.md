ASSUMPTIONS

- The system supports only a single user account.
- All data (bills, payments, balance) are stored in memory.
- Bill IDs are unique.
- If a bill is scheduled multiple times, the scheduled date will be updated instead of creating a new payment record.
- Scheduled payments are executed when the system checks and the scheduled date is reached.